# AI Website Builder ⚡

Built by Naman — Powered by Groq + Llama 70B

## Setup
1. Clone repo
2. `cd backend && npm install`
3. Copy `.env.example` to `.env` and add your GROQ_API_KEY
4. `npm start`

## Deploy
Railway → Connect GitHub → Add GROQ_API_KEY env var → Deploy